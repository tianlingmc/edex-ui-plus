# Electron 二进制镜像：解决国内网络无法从 GitHub 下载 Electron 的问题。
# npmmirror 全球可访问，提交到仓库不会影响海外 contributor。
# 注意：npm 11 不支持 `npm config set electron_mirror`（非合法 npm 配置项），
# 只能通过 .npmrc 或 ELECTRON_MIRROR 环境变量注入。
electron_mirror=https://registry.npmmirror.com/-/binary/electron/
